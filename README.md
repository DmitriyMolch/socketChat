# Minimal chat application with express.js, socket.io

#### Clone the git folder and run command:
```js
    npm i
    npm start
    // Go to localhost:3000
    // Register an account -> Login with the account -> Enter chat
```
